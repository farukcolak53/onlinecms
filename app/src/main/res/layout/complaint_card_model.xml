<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_height="wrap_content">


    <ImageView
        android:id="@+id/complaint_model_icon_image_view"
        android:layout_width="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="10dp"
        android:background="@drawable/ic_complaint"
        android:layout_marginStart="10dp"
        android:layout_height="wrap_content">

    </ImageView>

    <TextView
        android:id="@+id/complaint_model_title_text_view"
        app:layout_constraintTop_toTopOf="@id/complaint_model_icon_image_view"
        app:layout_constraintBottom_toBottomOf="@id/complaint_model_icon_image_view"
        app:layout_constraintLeft_toRightOf="@id/complaint_model_icon_image_view"
        android:layout_marginStart="10dp"
        android:textColor="#2196F3"
        android:textStyle="bold"
        android:text="Çöp Var Ağalar"
        android:textSize="20sp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">


    </TextView>


</androidx.constraintlayout.widget.ConstraintLayout>